Here it is:  ---  You are a senior full-stack developer. Build a complete Cybersecurity / OSINT Reconnaissance Platform with a clean modern dashboard UI using HTML, CSS, vanilla JavaScript for the frontend and Flask for the backend. The project must be fully structured, production-ready, and all features must be implemented end-to-end.  TECH STACK Frontend: HTML5, CSS3, vanilla JavaScript (no frameworks) Backend: Python Flask + Flask-Login + Flask-SQLAlchemy Database: SQLite (via SQLAlchemy) Auth: session-based with Flask-Login Exports: pdfkit or WeasyPrint for PDF, Python csv module for CSV  PROJECT STRUCTURE recon_platform/ app/ __init__.py - Flask app factory models.py - SQLAlchemy models auth/routes.py - login, logout, register targets/routes.py - CRUD for targets tools/routes.py - tool runner endpoints results/routes.py - results + reports notes/routes.py - notes CRUD export/routes.py - PDF and CSV export static/css/main.css static/js/main.js static/assets/ templates/ base.html - shared layout with sidebar nav auth/login.html auth/register.html dashboard/index.html - overview with stats cards targets/list.html targets/detail.html tools/runner.html results/viewer.html notes/index.html config.py requirements.txt run.py  DATABASE MODELS — implement all in models.py User — id, username, email, password_hash, created_at Target — id, name, domain/IP, type (domain/IP/org), status (active/archived), notes, created_at, user_id FK ScanResult — id, target_id FK, tool_name, raw_output, parsed_output JSON, created_at Note — id, target_id FK, user_id FK, content, created_at, updated_at  FEATURES — implement every one fully  1. User Authentication Register, login, logout with Flask-Login Password hashing with werkzeug All routes protected with login_required Redirect to login if unauthenticated  2. Target Management Add a target with name, domain/IP, type, description List all targets in a card grid with status badges View target detail page showing all scans and notes for that target Archive and delete a target Search and filter targets by name or type  3. Tool Runner A UI panel with a dropdown to select the tool and a text input for the target Tools to support: whois, nslookup, ping, traceroute, dig Run the selected tool via subprocess on the Flask backend Stream or return the raw output and display it in a terminal-style output box in the UI Save each result to the database linked to the selected target  4. Results Viewer Table view of all scan results, filterable by target and tool Click a row to expand and view the full raw output Show timestamp, tool name, target name per result  5. Notes and Annotations Add, edit, delete notes per target Notes displayed in a timeline style on the target detail page Plain textarea is fine, no rich text needed  6. Export Export all results for a target as CSV with tool, timestamp, output Export a full target report as PDF with target info, all scan results, and notes Export buttons visible on the target detail page  UI AND DESIGN REQUIREMENTS Clean modern dashboard aesthetic similar to linear.app or Vercel dashboard style Color palette: deep navy #0f1117 background, white cards #1c1f26, accent blue #4f8ef7, text #e2e8f0 Left sidebar with icons and labels: Dashboard, Targets, Tool Runner, Results, Notes Top bar with logged-in username and logout button Stat cards on dashboard showing Total Targets, Total Scans, Notes Count, Last Scan time All tables styled with hover states and alternating rows Responsive layout with sidebar collapsing on small screens Terminal output box with monospace font, dark background #0d1117, green text #39d353, auto-scrolls to bottom Smooth transitions and hover effects on all interactive elements Toast notifications for success and error actions in pure JS with no library  BACKEND REQUIREMENTS Flask app factory pattern in app/__init__.py All routes blueprinted and registered cleanly CSRF protection with Flask-WTF or manual token Tool runner uses subprocess.run() with a 10 second timeout and sanitises input to prevent shell injection All API responses from tool runner return JSON with output and saved_id fields Custom 404 and 500 error pages  DELIVERABLES Generate every file completely with no placeholders, no TODO comments, and no truncation. Every route, every template, every CSS rule, and every JS function must be fully written. Start with run.py, then config.py, then app/__init__.py, then models, then each blueprint, then all templates, then CSS, then JS, then requirements.txt.
